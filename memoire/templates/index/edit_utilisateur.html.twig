{% extends "base.html.twig" %}
{% block title %}Modfication Roles
{% endblock %}
{% block body %}
	<br>
	<div class="container">
		<div class="col-md-2"></div>
		<div class="col-md-8">
			{{form_start(formRoles)}}
			<div class="form-group">
				{{form_row(formRoles.username)}}
			</div>
			
			<div class="form-group">
				{{form_row(formRoles.roles)}}
			</div>
			<div class="form-group">
				<div class="btn-group">
					<a href="{{path('gestion_utulisateur')}}" class="btn btn-secondary">Annuler la modification
						<i class="fas fa-arrow-left"></i>
					</a>
					<button class="btn btn-success">Modifier</button>
				</div>
			</div>
			{{form_end(formRoles)}}
		</div>
		<div class="col-md-2"></div>

	</div>
{% endblock %}
